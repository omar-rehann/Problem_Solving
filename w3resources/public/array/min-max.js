/*


Array Min/Max Finder with Visualization
I'll create a complete web application that finds the minimum and maximum values in an array and visualizes the results.

*/

function L_S(array) {
    let test = Array.from(array);
    let min = Math.min(...test);
    let max = Math.max(...test);
    returnMin Mumber $ { min }
    and max number $ { max };
}
console.log(L_S([2, 3, 1, 100, 200, 1]))