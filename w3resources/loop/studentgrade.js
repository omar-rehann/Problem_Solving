/*

Student Grades Calculation
Write a JavaScript program that computes the average marks of the following students. 
Then, this average is used to determine the corresponding grade.
*/
function check(name, grade) {
    if (name == "" || grade == "") {
        alert("Please Try Again");
    } else if (grade <= 100 && grade >= 85) {
        return Suucsess $ { name }
        This is graet
    } else if (grade <= 85 && grade >= 75) {
        return Suucsess $ { name }
        This is good
    } else if (grade <= 75 && grade >= 60) {
        return Suucsess $ { name }
        This good and
        try increase grade in difrrent
        try
    } else {
        return "Falied"

    }
}
console.log(check("omar", 90))