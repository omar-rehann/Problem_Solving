// one problem
// Write a function that takes an integer as input and returns a string indicating whether the number is even or odd.
function Even_number(number) {
    return number % 2 === 0 ? " Even Number " : "Odd Number";
}
console.log(Even_number(1))