/*
2. Find Maximum and Minimum Values of a List

Question:
    Make two functions(max and min) that receive a list of integers and
return the largest and lowest number in that list.

Answer:
    *
*/

function max(list) {
    return Math.max(...list);
}

function min(list) {
    return Math.min(...list);
}