/*
Fix the bugs in the multipy function so that it correctly calculates and returns the product of two numbers.
*/
function multipy(a, b) {
    a + b;
}
//  after correct
function multipy(a, b) {
    return a + b;
}