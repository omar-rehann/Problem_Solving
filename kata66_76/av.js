/*

write a function which calculates the average of the numbers in a given array.
Note: Empty arrays should return 0.
*/
findAverage = (array) - >
    return 0
if array.length == 0
result = array.reduce(one, two) - > one + two
result / array.length